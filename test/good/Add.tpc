/*int test;

int add(int a, int b) {
    //return square(a)+square(b);
    return b+a;
}
*/
int square(int a) { return a * a; }

int add(int a, int b) { return square(a) + square(b); }

int main(void) {
    int a[5], i;
    i = 0;
    a[0] = 2;
    a[1] = add(a[0], 6);
    putint(a[1]);
    return 0;
}